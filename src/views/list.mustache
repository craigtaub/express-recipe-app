{{#dataArray.length}}
<input type="text" id="search" class="input-medium search-query"/>

<table class="table">
  {{#dataArray}}
  <tr>
    <td><a href="/item/{{id}}">{{name}}</a></td>
    <td>{{cookingTime}}</td>
    <td>{{ingredients.basic}}</td>
  </tr>
  {{/dataArray}}
</table>
<div style="display: none;" class="empty-msg">Sorry, nothing matched your filter term</div>
{{/dataArray.length}}

{{^dataArray.length}}
  <h2>Sorry, we currently have no recipes for you</h2>
{{/dataArray.length}}
